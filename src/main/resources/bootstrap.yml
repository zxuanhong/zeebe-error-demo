# Spring
spring:
  application:
    name: tasklist-service-ee
  cloud:
    nacos:
      password: anyicloudee
      username: anyicloudee
      server-addr: 162.14.71.212:8848
      discovery:
        namespace: anyicloudee
        metadata:
          management:
            context-path: ${server.servlet.context-path:}/actuator
        group: ${NACOS_GROUP:DEFAULT_GROUP}
      config:
        namespace: ${spring.cloud.nacos.discovery.namespace}
        # 配置文件格式
        file-extension: ${NACOS_CONFIG_FILE_EXTENSION:yml}
        name: ${NACOS_CONFIG_NAME:${spring.application.name}}
        enabled: ${CONFIG_ENABLE:false}
        group: ${spring.cloud.nacos.discovery.group}
        enable-remote-sync-config: true


# 端口与请求前缀
server:
  port: 605
  servlet:
    context-path: /tasklist
